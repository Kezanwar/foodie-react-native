import React from "react";
import { View } from "react-native";
import tw from "theme/tailwind";

type Props = {};

const DiscoverTabNavigator: React.FC<Props> = (props) => {
  return <View></View>;
};

export default DiscoverTabNavigator;
