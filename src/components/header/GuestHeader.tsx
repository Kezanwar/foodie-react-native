import { View, Text } from "react-native";
import React from "react";

type Props = {};

const GuestHeader = (props: Props) => {
  return (
    <View>
      <Text>GuestHeader</Text>
    </View>
  );
};

export default GuestHeader;
